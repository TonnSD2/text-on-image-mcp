# Copy to .env and fill in. NEVER commit .env.
# Public domain with an A/AAAA record pointing at this VPS.
TOI_DOMAIN=mcp.example.com

# token=workspace pairs. Generate tokens: openssl rand -hex 16
# Один клиент (рекомендованный старт): один токен = один workspace:
#   TOI_USERS=<ваш-сгенерированный-токен>=me
# Несколько клиентов — перечислить через запятую, у каждого свой
# изолированный workspace (свои сцены и файлы):
TOI_USERS=REPLACE_WITH_RANDOM_TOKEN_1=alice,REPLACE_WITH_RANDOM_TOKEN_2=bob

# Max scenes kept live in RAM (LRU; state itself persists on disk).
TOI_MAX_SCENES=16

# Remote/hosted mode: hide path-based tools (load_image/add_image);
# clients upload images as base64 via load_image_data/add_image_data.
TOI_REMOTE_MODE=1

# Image tag for the app (rollback = start previous tag).
TOI_TAG=dev-1